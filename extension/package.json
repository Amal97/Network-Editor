{
  "name": "network-modifier-extension",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Chrome DevTools extension for monitoring and modifying frontend XHR/fetch traffic.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.31",
    "esbuild": "^0.25.9",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "lucide": "^1.31.0"
  }
}
